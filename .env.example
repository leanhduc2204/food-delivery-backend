# Server
PORT=3001
NODE_ENV=development

# Database (required)
DB_USER=postgres
DB_PASSWORD=PostgreSQL@2026
DB_NAME=postgres
DATABASE_URL="postgresql://postgres:PostgreSQL@2026@localhost:5432/postgres?schema=public"

# JWT (required)
# Access token expiry (e.g. 15m, 1h, 1d)
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=15m
# Refresh token expiry (e.g. 7d)
JWT_REFRESH_EXPIRES_IN=7d

# Optional: separate refresh secret (if not set, JWT_SECRET is used for both)
# JWT_REFRESH_SECRET=optional-separate-refresh-secret
